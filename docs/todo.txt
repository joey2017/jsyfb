1.模型查询结果404异常捕获
2.日志目录设置，日志记录
3.接口文档语法
4.http请求状态码设定 (使用Symfony\Component\HttpFoundation\Response类的常量)
5.mysql事务处理
DB::transaction(function () {
    DB::table('users')->update(['votes' => 1]);

    DB::table('posts')->delete();
}, 5);

手动
DB::beginTransaction();
DB::rollBack();
DB::commit();
6.mysql模型查询链式操作